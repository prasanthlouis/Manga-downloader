This is pretty straightforward
Download get-pip.py (remember to right click and hit save as rather than left click the link)
Then simply run it by python get-pip.py through cmd
Then just wait for it to install
